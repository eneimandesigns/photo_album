<p id="notice"><%= notice %></p>

<h1><%= @album.title %></h1>

<p>
  <b>Directory:</b>
  <%= @album.directory %>
</p>

<p>
  <b>Content:</b>
  <%= @album.content %>
</p>

<p>
  <b>Created at:</b>
  <%= utc_time = Time.parse(@album.created_at.to_s).utc; utc_time.localtime; date = DateTime.parse(utc_time.localtime.to_s); date.strftime('%a %b %d %H:%M:%S') %>
</p>

<%= link_to 'Edit', edit_album_path(@album) %> |
<%= link_to 'Back', albums_path %>

<%= render :partial => 'photoset' %>
